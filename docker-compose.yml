version: '3.8'
services:
  cassandra:
    image: cassandra:latest
    container_name: cassandra-node
    ports:
      - "9042:9042"
    environment:
      - CASSANDRA_CLUSTER_NAME=BotDefenseCluster
    restart: always
